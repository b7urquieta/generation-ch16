alert("Hola Internauta, Mario necesita de tu ayuda para averiguar si un número es divisible entre 7 y 8, Ayudemosle!")


function calcularDivisibles(number1, number2); {

    let numberIngresado = prompt("Escribe el número en digitos")
    console.log(numberIngresado);
}{

    if (typeof number1 === "Divisible entre 7 FALSE" && typeof number === "Divisible entre 8 TRUE");
        console.log()
        let resultado = number1 / number2; 
        return resultado
    }

let operacion = calcularDivisibles(8 / 7)
console.log(calcularDivisibles);
